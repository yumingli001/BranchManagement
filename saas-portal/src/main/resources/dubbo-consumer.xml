<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd
        ">

    <dubbo:application name="${dubbo.application.name}">
        <dubbo:parameter key="qos.enable" value="false"/>
        <dubbo:parameter key="qos.accept.foreign.ip" value="false"/>
    </dubbo:application>

    <dubbo:registry address="${dubbo.registry.address}"/>

    <dubbo:consumer version="1.0.0" group="yunlsp.saas" timeout="3000"/>

    <!-- 生成远程服务代理，可以和本地bean一样使用 -->
    <dubbo:reference id="userLoginService" interface="com.yunlsp.saas.service.api.UserLoginService"/>
    <dubbo:reference id="userInfoService" interface="com.yunlsp.saas.service.api.UserInfoService"/>
    <dubbo:reference id="companyService" interface="com.yunlsp.saas.service.api.CompanyService"/>

</beans>